{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "pnpm db:migrate && echo MIG_OK && node -e \"const express=require('express'); const app=express(); app.get('/health',(_,r)=>r.json({ok:true})); app.listen(3000,()=>console.log('server_up'));\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
